/**
 * Reusable banner/card visual presets.
 * Keep these centralized so Home/Buy/Sell/Artisanal can share the same look.
 */

export const BANNER_CARD_BORDER = {
  borderWidth: 1,
  borderColor: '#E2E8F0',
};

export const BANNER_CARD_SHADOW = {
  shadowColor: 'rgba(0,0,0,0.1)',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 1,
  shadowRadius: 6,
  elevation: 4,
};
